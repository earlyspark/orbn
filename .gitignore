# Android / Gradle
*.iml
.gradle/
.kotlin/
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Per-module build outputs
**/build/

# IDE
.idea/
*.hprof

# Python spike venv (if any lands here)
.venv/

# Kept LOCAL for now (not published yet — strategy/spec + local tooling config)
/docs/
/.claude/

# Prebuilt native dependencies — fetched via scripts/fetch-native-deps.sh,
# kept out of git history (large binaries + vendored headers). See README.
/app/libs/
/app/src/main/jniLibs/
/app/src/main/assets/models/
